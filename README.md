# HandwritingToTextGemini
This project will convert your handwritten notes images to text files using Gemini Vision Pro model.
Install the dependencies: 
 pip install -r requirements.txt 

Run the project: 
python main.py